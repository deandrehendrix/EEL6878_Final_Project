"""Evaluation outline.

Purpose:
- Centralize test-set evaluation so both models are measured the same way.
- Compute classification metrics for node classification experiments.

Planned responsibilities:
- Compute accuracy on train/validation/test masks.
- Summarize results across repeated runs.
- Save metrics in a format that the final notebook can load.
"""
